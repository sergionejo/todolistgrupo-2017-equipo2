sudo: required

language: bash

services:
  - docker

before_install:
   - docker build -t madsequipo2/mads-equipo2-201718:latest .

script:
   - docker run --rm madsequipo2/mads-equipo2-201718:latest /bin/bash -c "sbt test"
